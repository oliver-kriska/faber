"""Faber eval sidecar.

The v1 boundary between the Elixir spine and the Python eval/optimizer ecosystem.
Invoked as ``python -m faber_eval <command>``: it reads a JSON request on stdin and
writes a JSON response on stdout. See :mod:`faber_eval.cli`.
"""

__version__ = "0.1.0"
