#!/usr/bin/env bash
# {{hook_name}} — {{description}}
#
# Generated by Faber from a hazard mined out of a real session: {{hazard}}
# Evidence: {{hazard_evidence}}
# Fires on: {{event}} / {{matcher}}
#
# Why a hook and not a skill: {{one_line_purpose}}
#
# Claude Code pipes the tool call to this script on stdin as JSON. Exit 0 allows the call;
# exit 2 blocks it and shows stderr to the agent.
set -uo pipefail

{{script}}
